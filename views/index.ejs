<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Intranet - Tableau de bord</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* --- CSS RESET & BASE --- */
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', sans-serif; }
        body { display: flex; background-color: #f4f6f8; min-height: 100vh; }
        
        /* --- SIDEBAR (MENU LATÉRAL) --- */
        /* Je n'ai pas touché au style pour garder la cohérence */
        .sidebar { width: 260px; background-color: #EAEAEA; display: flex; flex-direction: column; padding: 25px 0; height: 100vh; position: fixed; border-right: 1px solid #d1d1d1; z-index: 1000; }
        .logo { text-align: center; font-size: 28px; font-weight: 800; color: #000; margin-bottom: 40px; letter-spacing: 1px; }
        
        .menu-items { list-style: none; flex-grow: 1; }
        .menu-items a { display: flex; justify-content: flex-start; gap: 15px; align-items: center; padding: 12px 30px; text-decoration: none; color: #333; font-size: 15px; font-weight: 500; transition: 0.3s; }
        .menu-items a:hover { background-color: #d6d6d6; color: #000; }
        /* Classe active pour montrer qu'on est sur le dashboard */
        .menu-items li.active a { background-color: #d1d1d1; font-weight: 700; border-left: 4px solid #333; }
        
        .logout-item { margin-top: auto; margin-bottom: 20px; }

        /* --- CONTENU PRINCIPAL --- */
        .main-content { margin-left: 260px; padding: 40px; width: 100%; }

        /* --- BIENVENUE --- */
        .welcome-section { margin-bottom: 35px; }
        .welcome-section h1 { font-size: 28px; color: #2c3e50; }
        .welcome-section p { color: #7f8c8d; margin-top: 5px; }

        /* --- CARTES STATISTIQUES (WIDGETS) --- */
        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 25px; margin-bottom: 40px; }
        
        .stat-card { background: white; padding: 25px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); display: flex; align-items: center; gap: 20px; transition: transform 0.2s; border-bottom: 4px solid transparent; }
        .stat-card:hover { transform: translateY(-5px); }
        
        .stat-icon { width: 55px; height: 55px; border-radius: 12px; display: flex; align-items: center; justify-content: center; color: white; font-size: 24px; flex-shrink: 0; }
        
        .stat-info h3 { font-size: 28px; font-weight: 800; margin-bottom: 2px; color: #333; }
        .stat-info p { color: #666; font-size: 14px; font-weight: 500; }

        /* --- ACTUALITÉS --- */
        .section-title { margin-bottom: 20px; font-size: 22px; color: #333; border-left: 5px solid #333; padding-left: 15px; }
        
        .news-container { display: grid; gap: 15px; }
        .news-card { background: white; border-radius: 10px; padding: 20px; box-shadow: 0 2px 8px rgba(0,0,0,0.05); border-left: 4px solid #3498db; }
        .news-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px; }
        .news-title { font-weight: 700; font-size: 16px; color: #2c3e50; }
        .news-date { font-size: 12px; color: #95a5a6; background: #f0f2f5; padding: 4px 8px; border-radius: 20px; }
        .news-body { font-size: 14px; color: #555; line-height: 1.5; }
    </style>
</head>
<body>

    <nav class="sidebar">
        <div class="logo">ENTREPRISE</div>
        <ul class="menu-items">
            <li class="active"><a href="/employe-dashboard"><i class="fas fa-home"></i> Tableau de bord</a></li>
            <li><a href="/services"><i class="fas fa-concierge-bell"></i> Mes Services</a></li>
            <li><a href="/documents"><i class="fas fa-file-alt"></i> Documents</a></li>
            <li><a href="/departements"><i class="fas fa-building"></i> Départements</a></li>
            <li><a href="/applications"><i class="fas fa-laptop-code"></i> Applications</a></li>
            <li><a href="/apropos"><i class="fas fa-info-circle"></i> À propos</a></li>
            
            <li class="logout-item">
                <a href="/logout" style="color: #c0392b;">
                    <i class="fas fa-sign-out-alt"></i> Déconnexion
                </a>
            </li>
        </ul>
    </nav>

    <main class="main-content">
        
        <div class="welcome-section">
            <h1>Bonjour, <%= (typeof user !== 'undefined' && user) ? user.prenom + ' ' + user.nom : 'Collaborateur' %> 👋</h1>
            <p>Voici un aperçu de vos activités et de vos congés.</p>
        </div>

        <div class="stats-grid">
            
            <div class="stat-card" style="border-bottom-color: #9b59b6;">
                <div class="stat-icon" style="background-color: #9b59b6;"><i class="fas fa-umbrella-beach"></i></div>
                <div class="stat-info">
                    <h3><%= (typeof soldeConges !== 'undefined') ? soldeConges : 18 %></h3> 
                    <p>Jours de congés restants</p>
                </div>
            </div>

            <div class="stat-card" style="border-bottom-color: #e67e22;">
                <div class="stat-icon" style="background-color: #e67e22;"><i class="fas fa-tools"></i></div>
                <div class="stat-info">
                    <h3><%= (typeof nbTickets !== 'undefined') ? nbTickets : 0 %></h3>
                    <p>Tickets IT en cours</p>
                </div>
            </div>

            <div class="stat-card" style="border-bottom-color: #3498db;">
                <div class="stat-icon" style="background-color: #3498db;"><i class="fas fa-file-signature"></i></div>
                <div class="stat-info">
                    <h3><%= (typeof nbDemandesRH !== 'undefined') ? nbDemandesRH : 0 %></h3>
                    <p>Demandes RH en attente</p>
                </div>
            </div>

            <div class="stat-card" style="border-bottom-color: #e74c3c;">
                <div class="stat-icon" style="background-color: #e74c3c;"><i class="fas fa-bell"></i></div>
                <div class="stat-info">
                    <h3><%= (typeof nbNotifs !== 'undefined') ? nbNotifs : 0 %></h3>
                    <p>Nouvelles notifications</p>
                </div>
            </div>

        </div>

        <h2 class="section-title">Actualités de l'entreprise</h2>
        
        <div class="news-container">
            <% if(typeof annonces != 'undefined' && annonces && annonces.length > 0) { %>
                <% annonces.forEach(function(annonce) { %>
                    <div class="news-card">
                        <div class="news-header">
                            <span class="news-title"><%= annonce.titre %></span>
                            <span class="news-date"><i class="far fa-calendar-alt"></i> <%= new Date(annonce.date_publication).toLocaleDateString('fr-FR') %></span>
                        </div>
                        <div class="news-body">
                            <%= annonce.message %>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="news-card" style="border-left-color: #ccc;">
                    <p style="color: #666; font-style: italic;">Aucune nouvelle actualité pour le moment.</p>
                </div>
            <% } %>
        </div>

    </main>

</body>
</html>