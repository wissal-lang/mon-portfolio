<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Intranet - Tableau de bord</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* --- 1. CSS DE BASE --- */
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', sans-serif; }
        body { display: flex; background-color: #f4f6f8; min-height: 100vh; }
        
        /* MENU LATÉRAL */
        .sidebar { width: 260px; background-color: #EAEAEA; display: flex; flex-direction: column; padding: 25px 0; height: 100vh; position: fixed; border-right: 1px solid #d1d1d1; }
        .logo { text-align: center; font-size: 28px; font-weight: 800; color: #000; margin-bottom: 40px; letter-spacing: 1px; }
        
        .menu-items { list-style: none; flex-grow: 1; }
        .menu-items a { display: flex; justify-content: flex-start; gap: 15px; align-items: center; padding: 12px 30px; text-decoration: none; color: #333; font-size: 15px; font-weight: 500; transition: 0.3s; }
        .menu-items a:hover { background-color: #d6d6d6; color: #000; }
        .menu-items li.active a { background-color: #d1d1d1; font-weight: 700; border-left: 4px solid #333; }
        
        .logout-item { margin-top: auto; margin-bottom: 20px; }

        /* CONTENU PRINCIPAL */
        .main-content { margin-left: 260px; padding: 50px; width: 100%; }

        /* ACTUALITÉS */
        .section-title { margin-bottom: 20px; font-size: 20px; border-left: 5px solid #3498db; padding-left: 10px; color: #2c3e50; }
        
        .news-card { 
            background: white; 
            border-radius: 8px; 
            padding: 20px; 
            box-shadow: 0 2px 5px rgba(0,0,0,0.05); 
            margin-bottom: 20px; 
            border-left: 4px solid #f39c12; /* Bordure orange pour faire ressortir l'info */
            transition: transform 0.2s;
        }
        .news-card:hover { transform: translateY(-2px); }

        .news-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 10px; }
        .news-title { font-weight: bold; font-size: 18px; color: #2c3e50; }
        .news-date { font-size: 13px; color: #7f8c8d; display: flex; align-items: center; gap: 5px; }
        
        /* Important pour garder les paragraphes du message */
        .news-content { color: #555; line-height: 1.6; white-space: pre-line; }
    </style>
</head>
<body>

    <nav class="sidebar">
        <div class="logo">ENTREPRISE</div>
        <ul class="menu-items">
            <li class="active"><a href="/employe-dashboard"><i class="fas fa-home"></i> Tableau de bord</a></li>
            <li><a href="/services"><i class="fas fa-concierge-bell"></i> Mes Services</a></li>
            <li><a href="/documents"><i class="fas fa-file-alt"></i> Documents</a></li>
            <li><a href="/departements"><i class="fas fa-building"></i> Départements</a></li>
            <li><a href="/applications"><i class="fas fa-laptop-code"></i> Applications</a></li>
            <li><a href="/apropos"><i class="fas fa-info-circle"></i> À propos</a></li>
            
            <li class="logout-item">
                <a href="/logout" style="color: #c0392b;">
                    <i class="fas fa-sign-out-alt"></i> Déconnexion
                </a>
            </li>
        </ul>
    </nav>

    <main class="main-content">
        
        <div style="margin-bottom: 30px;">
            <h1>Bonjour, <%= (typeof user !== 'undefined' && user && user.prenom) ? user.prenom : 'Employé' %> 👋</h1>
            <p style="color: #666; margin-top: 5px;">Bienvenue sur votre espace intranet<%= (typeof user !== 'undefined' && user && user.nom) ? ', ' + user.nom : '' %>.</p>
        </div>

        <h2 class="section-title"><i class="fas fa-bullhorn"></i> Actualités & Annonces</h2>
        
        <% if(typeof annonces != 'undefined' && annonces && annonces.length > 0) { %>
            <% annonces.forEach(function(annonce) { %>
                <div class="news-card">
                    <div class="news-header">
                        <div class="news-title"><%= annonce.titre %></div>
                        <span class="news-date">
                            <i class="far fa-clock"></i>
                            <%= new Date(annonce.date_publication).toLocaleDateString('fr-FR', { 
                                day: 'numeric', month: 'long', hour: '2-digit', minute: '2-digit' 
                            }) %>
                        </span>
                    </div>
                    <p class="news-content"><%= annonce.message %></p>
                </div>
            <% }); %>
        <% } else { %>
            <div style="text-align: center; padding: 40px; background: white; border-radius: 8px; color: #999;">
                <i class="fas fa-inbox" style="font-size: 30px; margin-bottom: 10px; display: block;"></i>
                <p>Aucune nouvelle actualité pour le moment.</p>
            </div>
        <% } %>

    </main>

</body>
</html>